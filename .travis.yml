language: cpp
sudo: required
dist: trusty
compiler:
- gcc
os:
- linux
ruby: 2.2.0
before_install:
- date
- locale
- echo $PATH
install:
- apt-get install libprotobuf-dev protobuf-compiler
before_script: false
script:
- make test
after_success: false
branches:
  only:
  - master
notifications:
  email:
    recipients:
    - leoxiang727@gmail.com
env:
  global:
  - LANG="en_US.UTF-8"
